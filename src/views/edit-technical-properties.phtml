<?php include_once 'src/templates/header.phtml'; ?>

<main class="min-h-screen bg-indigo-100 w-full grid items-start py-8">
    <div class="max-w-2xl mx-auto bg-white shadow-md rounded-lg p-8">
        
        <div class="mb-6">
            <h2 class="text-2xl font-semibold text-indigo-700">Editar Ficha Técnica</h2>
            <p class="text-gray-600">Propiedad: <?= htmlspecialchars($propertyAddress) ?></p>
        </div>

        <form action="<?= BASE_URL ?>actualizar-datos-tecnicos" method="POST">
            <input type="hidden" name="id_technical_properties" value="<?= $technical->id_technical_properties ?>">

            <div>
                <label class="text-gray-700">Nomenclatura Catastral</label>
                <input type="text" name="cadastral_nomenclature" required
                    value="<?= htmlspecialchars($technical->cadastral_nomenclature) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">Número de Partida</label>
                <input type="number" name="departure_number" required
                    value="<?= htmlspecialchars($technical->departure_number) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">Topografía</label>
                <input type="text" name="topography" required
                    value="<?= htmlspecialchars($technical->topography) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">Parcela</label>
                <input type="number" name="parcel" required
                    value="<?= htmlspecialchars($technical->parcel) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="text-gray-700">Metros de Frente</label>
                    <input type="number" step="0.01" name="meters_front" required
                        value="<?= htmlspecialchars($technical->meters_front) ?>"
                        class="w-full border border-gray-300 rounded-md px-4 py-2" />
                </div>
                <div>
                    <label class="text-gray-700">Metros de Fondo</label>
                    <input type="number" step="0.01" name="meters_deep" required
                        value="<?= htmlspecialchars($technical->meters_deep) ?>"
                        class="w-full border border-gray-300 rounded-md px-4 py-2" />
                </div>
            </div>

            <div>
                <label class="text-gray-700">Acceso</label>
                <input type="text" name="access" required
                    value="<?= htmlspecialchars($technical->access) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">Servicios</label>
                <textarea name="services" rows="3" required
                    class="w-full border border-gray-300 rounded-md px-4 py-2"><?= htmlspecialchars($technical->services) ?></textarea>
            </div>

            <div>
                <label class="text-gray-700">Ocupada</label>
                <select name="busy" class="w-full border border-gray-300 rounded-md px-4 py-2" required>
                    <option value="0" <?= $technical->busy == 0 ? 'selected' : '' ?>>No</option>
                    <option value="1" <?= $technical->busy == 1 ? 'selected' : '' ?>>Sí</option>
                </select>
            </div>

            <div>
                <label class="text-gray-700">Superficie (m²)</label>
                <input type="text" name="surface" required
                    value="<?= htmlspecialchars($technical->surface) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">Comodidades / Amenities</label>
                <textarea name="amenities" rows="3" required
                    class="w-full border border-gray-300 rounded-md px-4 py-2"><?= htmlspecialchars($technical->amenities) ?></textarea>
            </div>

            <div>
                <label class="text-gray-700">Tipo de Ajuste</label>
                <input type="text" name="adjustment_type" required
                    value="<?= htmlspecialchars($technical->adjustment_type) ?>"
                    class="w-full border border-gray-300 rounded-md px-4 py-2" />
            </div>

            <div>
                <label class="text-gray-700">¿Es Nueva?</label>
                <select name="is_new" class="w-full border border-gray-300 rounded-md px-4 py-2" required>
                    <option value="0" <?= $technical->is_new == 0 ? 'selected' : '' ?>>No</option>
                    <option value="1" <?= $technical->is_new == 1 ? 'selected' : '' ?>>Sí</option>
                </select>
            </div>

            <div class="flex justify-between mt-8">
                <a href="<?= BASE_URL ?>datos-tecnicos/<?= $technical->id_technical_properties ?>"
                    class="px-4 py-2 bg-gray-200 text-gray-700 rounded-md hover:bg-gray-300 transition-colors">
                    Cancelar
                </a>

                <button type="submit"
                    class="px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 transition-colors">
                    Guardar Cambios
                </button>
            </div>

        </form>
    </div>
</main>

<?php include_once 'src/templates/footer.phtml'; ?>