<?php include_once 'src/templates/header.phtml'; ?>

<main class="min-h-screen bg-indigo-100 w-full grid items-start gap-4 py-4">

    <div class="container mx-auto flex justify-between items-center px-4">
        <a href="<?= BASE_URL ?>inicio"
            class="inline-block transform transition-transform duration-100 hover:scale-95 bg-red-700 text-white border border-transparent rounded-md px-4 py-2 no-underline hover:bg-red-700 shadow-sm font-medium">
            Regresar al inicio
        </a>

        <h1 class="text-3xl font-bold text-indigo-700 tracking-tight">
            Gestión de Alquileres
        </h1>

        <a href="<?= BASE_URL ?>crear-alquiler"
            class="inline-block transform transition-transform duration-100 hover:scale-95 bg-green-700 text-white border border-transparent rounded-md px-4 py-2 no-underline hover:bg-green-700 shadow-sm font-medium">
            Crear alquiler
        </a>
    </div>

    <div class="container flex justify-center mx-auto">
        <div class="flex flex-col w-full px-4">
            <div class="border-b border-gray-200 shadow rounded-lg overflow-hidden">
                <table class="divide-y divide-gray-300 w-full">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Inquilino</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Dirección Propiedad</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Inicio Contrato</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Monto</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">
                                Estado
                            </th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-300">
                        <?php foreach ($rents as $r): ?>
                            <tr class="whitespace-nowrap hover:bg-indigo-50 transition-colors duration-150">
                                <td class="px-4 py-3 font-medium text-gray-900"><?= htmlspecialchars($r->tenant_name) ?> <?= htmlspecialchars($r->tenant_lastname) ?></td>
                                <td class="px-4 py-3 text-gray-600"><?= htmlspecialchars($r->property_address) ?></td>
                                <td class="px-4 py-3 text-gray-600"><?= date("d/m/Y", strtotime($r->start_contract)) ?></td>
                                <td class="px-4 py-3">
                                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                                        $ <?= number_format($r->amount, 0, ',', '.') ?>
                                    </span>
                                </td>
                                <td class="px-4 py-3 text-gray-600">
                                    <?php
                                    if ($r->last_payment_date) {
                                        $lastPay = date("Y-m", strtotime($r->last_payment_date));
                                        $currentMonth = date("Y-m");

                                        if ($lastPay === $currentMonth) {
                                            echo '<span class="text-green-700 font-semibold">Al día (' . date("m/Y", strtotime($r->last_payment_date)) . ')</span>';
                                        } else {
                                            echo '<span class="text-red-600 font-semibold">Atrasado (' . date("m/Y", strtotime($r->last_payment_date)) . ')</span>';
                                        }
                                    } else {
                                        echo '<span class="text-red-700 font-bold">Sin pagos</span>';
                                    }
                                    ?>
                                </td>

                                <td class="px-4 py-3 flex justify-center gap-2">
                                    <a href="/pifano-inmobiliaria/control-alquiler/<?= $r->id_rent ?>"
                                        class="inline-block transform transition-transform duration-100 hover:scale-95 px-3 py-1 text-sm font-medium text-green-700 bg-green-100 rounded-md hover:bg-green-200">
                                        Pagos
                                    </a>
                                    <a href="/pifano-inmobiliaria/editar-alquiler/<?= $r->id_rent ?>"
                                        class="inline-block transform transition-transform duration-100 hover:scale-95 px-3 py-1 text-sm font-medium text-indigo-700 bg-indigo-100 rounded-md hover:bg-indigo-200">
                                        Editar
                                    </a>
                                    <a onclick="return confirm('¿Seguro que deseas eliminar este alquiler?');"
                                        href="?action=eliminar-alquiler/<?= $r->id_rent ?>"
                                        class="inline-block transform transition-transform duration-100 hover:scale-95 px-3 py-1 text-sm font-medium text-red-700 bg-red-100 rounded-md hover:bg-red-200">
                                        Eliminar
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>

                        <?php if (empty($rents)): ?>
                            <tr>
                                <td colspan="6" class="px-6 py-4 text-center text-gray-500 italic">
                                    No hay alquileres registrados.
                                </td>
                            </tr>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>


<?php include_once 'src/templates/footer.phtml'; ?>