<?php include_once 'src/templates/header.phtml'; ?>

<main class="min-h-screen bg-indigo-100 w-full grid items-start gap-4 py-4">

    <div class="container mx-auto flex justify-between items-center px-4">

        <a href="<?= BASE_URL ?>inicio"
            class="inline-block transform transition-transform duration-100 hover:scale-95 bg-red-700 text-white border border-transparent rounded-md px-4 py-2 no-underline hover:bg-red-700 shadow-sm font-medium">
            Regresar al inicio
        </a>

        <h1 class="text-3xl font-bold text-indigo-700 tracking-tight">
            Gestión de Clientes
        </h1>

        <a href="<?= BASE_URL ?>crear-cliente"
            class="inline-block transform transition-transform duration-100 hover:scale-95 bg-green-700 text-white border border-transparent rounded-md px-4 py-2 no-underline hover:bg-green-700 shadow-sm font-medium">
            Nuevo Cliente
        </a>
    </div>

    <div class="container flex justify-center mx-auto">
        <div class="flex flex-col w-full px-4">
            <div class="border-b border-gray-200 shadow rounded-lg overflow-hidden">
                <table class="divide-y divide-gray-300 w-full">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Nombre</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Apellido</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Email</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Teléfono</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">DNI</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-left">Dirección</th>
                            <th class="px-4 py-3 text-xs font-bold text-gray-500 uppercase tracking-wider text-center">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-300">
                        <?php foreach ($Clients as $client): ?>
                            <tr class="whitespace-nowrap hover:bg-indigo-50 transition-colors duration-150">
                                <td class="px-4 py-3 font-medium text-gray-900"><?= htmlspecialchars($client->name) ?></td>
                                <td class="px-4 py-3 font-medium text-gray-900"><?= htmlspecialchars($client->lastname) ?></td>
                                <td class="px-4 py-3 text-gray-600"><?= htmlspecialchars($client->email) ?></td>
                                <td class="px-4 py-3 text-gray-600"><?= htmlspecialchars($client->phone) ?></td>
                                <td class="px-4 py-3">
                                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                                        <?= htmlspecialchars($client->dni) ?>
                                    </span>
                                </td>
                                <td class="px-4 py-3 text-gray-600"><?= htmlspecialchars($client->address) ?></td>
                                <td class="px-4 py-3 flex justify-center gap-2">
                                    <a href="/pifano-inmobiliaria/editar-cliente/<?= $client->id_client ?>"
                                        class="inline-block transform transition-transform duration-100 hover:scale-95 px-3 py-1 text-sm font-medium text-indigo-700 bg-indigo-100 rounded-md hover:bg-indigo-200">
                                        Editar
                                    </a>

                                    <a onclick="return confirm('¿Seguro que deseas eliminar este cliente?');"
                                        href="/pifano-inmobiliaria/eliminar-cliente/<?= $client->id_client ?>"
                                        class="inline-block transform transition-transform duration-100 hover:scale-95 px-3 py-1 text-sm font-medium text-red-700 bg-red-100 rounded-md hover:bg-red-200">
                                        Eliminar
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>

                        <?php if (empty($Clients)): ?>
                            <tr>
                                <td colspan="7" class="px-6 py-4 text-center text-gray-500 italic">
                                    No se encontraron clientes registrados.
                                </td>
                            </tr>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>

<?php include_once 'src/templates/footer.phtml'; ?>